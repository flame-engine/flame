name: flame_test_example
description: Example of how to test with the flame_test package
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=2.14.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  flame: ^1.1.0
dev_dependencies:
  flame_test: ^1.2.0
  flame_lint: ^0.0.1

flutter:
  assets:
    - assets/images/
