name: flame_test_example
description: Example of how to test with the flame_test package
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=2.16.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  flame: ^1.1.1
dev_dependencies:
  flame_test:
    path: ../
  flame_lint: ^0.0.1

flutter:
  assets:
    - assets/images/
