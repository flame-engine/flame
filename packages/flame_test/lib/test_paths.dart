/// A set of paths with different kinds of contours, for tests, benchmarks and
/// examples of code that works on paths.
///
/// This library only depends on Flame, so that an app can use the paths
/// without depending on the test frameworks that the rest of `flame_test`
/// uses.
library;

export 'src/test_paths.dart';
