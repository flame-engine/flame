name: example
description: A flame network images example.
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=2.18.0 <3.0.0'

dependencies:
  flutter:
    sdk: flutter
  flame: ^1.6.0
  flame_network_image:
    path: ../
  cupertino_icons: ^1.0.2
