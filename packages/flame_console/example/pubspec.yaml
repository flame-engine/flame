name: flame_console_example
resolution: workspace
description: "Example of using the flame console package"

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=3.6.0 <4.0.0"

dependencies:
  flame: ^1.29.0
  flame_console: ^0.1.2+8
  flutter:
    sdk: flutter

dev_dependencies:
  flame_lint: ^1.4.0
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
