name: flame_kenney_xml_example
resolution: workspace
description: "An example for the `XmlSpriteSheet` used to load kenney.nl assets."

publish_to: "none"

version: 1.0.0

environment:
  sdk: ">=3.6.0 <4.0.0"

dependencies:
  flame: ^1.26.0
  flame_kenney_xml: ^0.1.1+7
  flutter:
    sdk: flutter

dev_dependencies:
  flame_lint: ^1.2.3

flutter:
  assets:
    - assets/
    - assets/images/
