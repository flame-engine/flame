name: flutter_isolates_example
description: Flame game example for running threaded workloads

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=2.18.0 <3.0.0'

dependencies:
  collection: ^1.16.0
  flame: ^1.3.0
  flame_isolate:
    git:
      url: git@github.com:lohnn/flame.git
      ref: feat/flame_isolate
      path: packages/flame_isolate
  flutter:
    sdk: flutter
  integral_isolates:
  quiver: ^3.1.0

dev_dependencies:
  flame_lint: ^0.1.2

dependency_overrides:
  integral_isolates:
    path: /Users/lohnn/Programming/integral_isolates/packages/integral_isolates

flutter:
  assets:
    - assets/images/
