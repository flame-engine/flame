name: flame_isolate_example
resolution: workspace
description: Flame game example for running threaded workloads

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=3.8.0 <4.0.0"
  flutter: ">=3.32.0"

dependencies:
  collection: ^1.18.0
  flame: ^1.31.0
  flame_isolate: ^0.6.2+15
  flutter:
    sdk: flutter
  quiver: ^3.2.1

dev_dependencies:
  flame_lint: ^1.4.2
flutter:
  assets:
    - assets/images/
