name: sequence_effect
description: Flame sample game showcasing the sequence effect

version: 0.1.0
publish_to: none

environment:
  sdk: ">=2.1.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  flame:
    path: ../../../../

dev_dependencies:
  flutter_test:
    sdk: flutter
